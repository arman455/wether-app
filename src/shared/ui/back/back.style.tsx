import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    main: {width: "100%", alignItems: "flex-start", padding: 10},
    container: { alignItems: "center", justifyContent: "center", gap: 5, flexDirection: "row" },
    text: { fontSize: 16}
})
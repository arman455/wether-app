
import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    main: {
        flex: 1,
        alignItems: 'center',
        gap: 10,
    },
    container: {
        flexGrow: 1,
        alignItems: 'center',
        justifyContent: "flex-start",
        gap: 10,
        overflow: "hidden",
    },

})